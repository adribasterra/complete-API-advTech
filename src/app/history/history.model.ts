
export class History {
  id: number;
  date: Date;
  idcustomer: number;
  age: number;        // From customer birthdate
  idstore: number;
  sector: string;     // The store sector
  idprize: number;
  points: number;     // The prize points
  name: string;       // The prize name
  category: string;   // The prize category
}